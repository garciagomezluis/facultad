Sobre campus:
============

	g++ -g test.cpp campus.cpp -o tests

Para correr valgrind (sobre el binario anterior):

	valgrind --leak-check=full -v ./tests

====================================================================
Sobre diccRapido
================

	g++ -g test.cpp -o test

Para correr valgrind (sobre el binario anterior):

	valgrind --leak-check=full -v ./test